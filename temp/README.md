# temp

此目录统一存放开发过程中产生的临时文件，例如截图、视觉审阅导出、诊断结果和一次性测试输入。
`npm run motion:audit` 的默认输出位于 `temp/motion-audit/`。

- 除本说明外，目录内容默认由 Git 忽略。
- 正式运行资源、测试 fixture 和需要长期维护的文档不得放在此处。
- 临时文件可按任务建立子目录；确认需要进入产品或自动化测试后，再移动到对应模块的正式目录。
