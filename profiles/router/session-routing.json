{
  "version": 1,
  "name": "session-routing",
  "instructions": [
    "判断当前消息应发送给桌面角色，还是发送给候选任务会话。",
    "只能选择请求中给出的候选 sessionId；没有合适候选时返回 no-match。",
    "任务会话补充默认只处理当前最后一条用户请求，不要求等待上一轮回复结束。",
    "仅在多个候选确实接近且无法可靠区分时请求用户确认。",
    "必须对输入中的每个候选会话给出分数和简短理由。",
    "不要执行工具、提交任务或生成面向用户的回复；只返回结构化路由建议。"
  ]
}
